# Local backend API (Express/FastAPI running on local machine)
VITE_API_URL=http://localhost:3000/api

# ML classify endpoint — point to local mock or deployed API Gateway
VITE_CLASSIFY_URL=http://localhost:3000/api
