# EC2 backend API (Nginx reverse-proxies /api to the backend process)
VITE_API_URL=/api

# ML classify endpoint — AWS API Gateway + Lambda
# Replace with actual API Gateway URL once deployed
VITE_CLASSIFY_URL=https://REPLACE_ME.execute-api.us-east-1.amazonaws.com/prod
